diff --git a/src/background/capture-data-cleanup-service.ts b/src/background/capture-data-cleanup-service.ts
index 3e7e93c..4d80ba0 100644
--- a/src/background/capture-data-cleanup-service.ts
+++ b/src/background/capture-data-cleanup-service.ts
@@ -20,11 +20,14 @@ export interface CaptureOwnedDataCleanupPort {
 }
 
 export interface CaptureOwnedDataCleanupServiceOptions {
-  jobs: JobRepositoryPort;
-  sessions: JobSessionRepositoryPort;
-  tiles: TileRepositoryPort;
+  jobs: Pick<JobRepositoryPort, "delete">;
+  sessions: Pick<
+    JobSessionRepositoryPort,
+    "getSummary" | "getTabLock" | "deleteJob"
+  >;
+  tiles: Pick<TileRepositoryPort, "deleteByJob">;
   artifacts: JobArtifactCleanupPort;
-  manifests: PdfEditManifestRepositoryPort;
+  manifests: Pick<PdfEditManifestRepositoryPort, "load" | "delete">;
 }
 
 function cleanupWarning(jobId: string, failedOperations: string[]): WebCapErrorData | undefined {
diff --git a/tests/unit/capture-data-cleanup-service.test.ts b/tests/unit/capture-data-cleanup-service.test.ts
index 5dee82e..3ba5aab 100644
--- a/tests/unit/capture-data-cleanup-service.test.ts
+++ b/tests/unit/capture-data-cleanup-service.test.ts
@@ -49,8 +49,8 @@ describe("CaptureOwnedDataCleanupService", () => {
       deletedManifests: 1,
       clearedSessions: 1,
     });
-    expect(current.manifests.delete).toHaveBeenCalledWith("job-1");
-    expect(current.sessions.deleteJob).toHaveBeenCalledWith("job-1");
+    expect(vi.mocked(current.manifests.delete)).toHaveBeenCalledWith("job-1");
+    expect(vi.mocked(current.sessions.deleteJob)).toHaveBeenCalledWith("job-1");
   });
 
   it("continues best-effort cleanup and returns a safe partial warning", async () => {
@@ -65,6 +65,6 @@ describe("CaptureOwnedDataCleanupService", () => {
       code: "E_CLEANUP_PARTIAL",
       causeCode: "CaptureOwnedDataCleanupPartial",
     });
-    expect(current.sessions.deleteJob).toHaveBeenCalledTimes(1);
+    expect(vi.mocked(current.sessions.deleteJob)).toHaveBeenCalledTimes(1);
   });
 });
diff --git a/tests/unit/capture-reset-service.test.ts b/tests/unit/capture-reset-service.test.ts
index 07287e5..cef9294 100644
--- a/tests/unit/capture-reset-service.test.ts
+++ b/tests/unit/capture-reset-service.test.ts
@@ -135,9 +135,9 @@ describe("CaptureResetService", () => {
 
     expect(current.captures.cancel).toHaveBeenCalledWith("job-1", "capture reset", "discard");
     expect(current.captures.waitForIdle).toHaveBeenCalledWith("job-1");
-    expect(vi.mocked(current.cleanup.cleanupJob).mock.invocationCallOrder[0]).toBeGreaterThan(
-      current.captures.waitForIdle.mock.invocationCallOrder[0],
-    );
+    expect(
+      vi.mocked(current.cleanup.cleanupJob).mock.invocationCallOrder[0] ?? 0,
+    ).toBeGreaterThan(current.captures.waitForIdle.mock.invocationCallOrder[0] ?? 0);
   });
 
   it("is idempotent when the job record is already missing", async () => {
@@ -146,7 +146,7 @@ describe("CaptureResetService", () => {
       createCaptureResetRequest({ requestId: "reset-missing", sentAt: now, scope: "job", jobId: "missing" }),
     );
     expect(report).toMatchObject({ deletedJobs: 0, cancellationAttempted: false });
-    expect(current.cleanup.cleanupJob).toHaveBeenCalledWith("missing", undefined);
+    expect(vi.mocked(current.cleanup.cleanupJob)).toHaveBeenCalledWith("missing", undefined);
   });
 
   it("cancels visible processing, removes all artifacts linked to the source, and clears session", async () => {
@@ -176,7 +176,7 @@ describe("CaptureResetService", () => {
 
     expect(current.imageExport.cancelBySourceArtifactId).toHaveBeenCalledWith("capture-1");
     expect(current.artifactsByJob.deleteByJob).toHaveBeenCalledWith("capture-1");
-    expect(current.visibleCapture.releaseCapture).toHaveBeenCalledWith("capture-1");
+    expect(vi.mocked(current.visibleCapture.releaseCapture)).toHaveBeenCalledWith("capture-1");
     expect(current.getVisibleSession()).toBeUndefined();
     expect(report).toMatchObject({
       cancellationAttempted: true,
